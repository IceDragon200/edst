#!/usr/bin/env ruby
# edst-2toml
# uses a subset of the EDST markup to generate a kvp map
require 'edst/cli/edst-2common'
require 'edst/core_ext/toml'
require 'toml'

EDST::Cli::Convert.run(ARGV.dup) do |data|
  [:text, data.to_toml]
end
